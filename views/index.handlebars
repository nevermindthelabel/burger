<div class="container" id="header">
  <div class="row">
    <div class="col-md-12">
      <h1>Devour the Burgers!</h1>
    </div>
  </div>
</div>
<div class="container" id="background">
  <div class="row">
    <div class="col-md-6">
      <h2>Burgers that you'd like to devour</h2>
      {{#each burgers}}
      {{#unless this.devoured}}
      <p>{{this.burger_name}}</p>
      <form class="update-burger">
        <button class="btn btn-primary devour" type="submit" data-id="{{this.id}}">Devour
          {{this.burger_name}} </button>
      </form>

      {{/unless}}
      {{/each}}
      <br />

      {{!-- <h3>Add a burger to devour!</h3>
      <form class="create-form">
        <div class="form-group">
          <label for="burger">Burger to add:</label>
          <input type="text" id='burger' name="name">
        </div>
        <button class="burger" type="submit">Add your burger</button>
      </form> --}}

      <h2>Add A Burger Option</h2>

      <div class="row">
        <div class="col s12">
          <form action="/api/burgers" method="POST">
            <input type="text" id="name" name="burger_name" placeholder="Enter Your Burger">
            <button type="submit" class="addBurgerButton">Add The Burger</button>
          </form>
        </div>
      </div>

    </div>
    <div class="col-md-6">
      <h2>Burgers that have already been devoured:</h2>
      <ul>
        {{#each burgers}}
        {{#if this.devoured}}
        <h5>{{this.burger_name}}</h5>
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
</div>


{{!-- <h1>Eat-Da-Burger!</h1>

<main>
  <div class="container">
    <div class="row">
      <div class="col s6">
        {{#each burgers}}
        {{#unless this.devoured}}
        <div>
          <form action="/api/burgers/{{this.id}}?_method=POST" method="POST">
            <button type="submit" class='burgerAvailable'> {{this.burger_name}}</button>
          </form>
        </div>
        {{/unless}}
        {{/each}}
      </div> --}}
      {{!--
      <div class="col s6">
        {{#each burgers}}
        {{#if this.devoured}}
        <div class='burgerEaten'>
          <div>
            {{this.burger_name}}
          </div>
        </div>
        {{/if}}
        {{/each}}
      </div>
    </div>
  </div> --}}
  {{!--
</main> --}}

{{!-- <h2>Add A Burger Option</h2>

<div class="row">
  <div class="col s12">
    <form action="/api/burgers" method="POST">
      <input type="text" id="name" name="burger_name" placeholder="Enter Your Burger">
      <button type="submit" class="addBurgerButton">Add The Burger</button>
    </form>
  </div>
</div> --}}

{{!-- {
{
!--link jQuery
listen to the submit event from the form
prevent default
get the data from the form
send the request
reload the page
--}
} --}}